package linkedlnQuizApril2024;

public class Test18April2024 {

	/*
	 * enum Enums Extends A{
	 *  ABC,BCD,CDE,DEF; 
	 *  }
	 * The provided Java code will not be compiled successfully.
	 * Enum types cannot extend a class in Java. 
	 * Enums can only directly extend the java.lang.
	 * Enum class implicitly, which is done automatically by the compiler. 
	 * So, trying to extend a class like A in your example will result in a compilation error.
	 */
		
}
